{В одномерном массиве целых чисел найти сумму всех цифр целочисленного массива}
var a:array [1..100] of integer;
i,n,s,t:integer;
begin
  randomize;
  readln(n);
  s:=0;
  for i:=1 to n do begin
    a[i]:=random(1999)-999;
    writeln(a[i]);
  end;
  for i:=1 to n do begin
    if a[i]<0 then a[i]:=a[i]*(-1);
    if a[i]>9 then begin
      while a[i]>0 do begin
         s:=s+a[i] mod 10;
         a[i]:=a[i] div 10;
      end;
    end else s:=s+a[i];
  end;
  writeln(s);
  readln;
end.
